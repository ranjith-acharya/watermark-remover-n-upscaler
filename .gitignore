# Virtual environment
.venv/
venv/

# Downloaded model weights (fetched on first use). The measured Flow sparkle
# is small and ships with the repo, so nobody has to calibrate it themselves.
models/*
!models/flow_glyph.npz

# Calibration images belong to whoever generated them
samples/

# Working directories
output/
uploads/

# Sample media - keep generated clips out of the repo
*.mp4
*.mov
*.mkv
*.webm

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Tooling scratch
.code-review-graph/
